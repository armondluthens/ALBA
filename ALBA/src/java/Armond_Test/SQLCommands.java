/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package Armond_Test;

/**
 *
 * @author armondluthens
 */
public class SQLCommands {
    
/*------------------------------------------------------

SELECT * FROM USERS;
SELECT * FROM ADMIN_CODES;
SELECT * FROM MANAGER_CODES;

INSERT INTO USERS (Role)
VALUES ('Admin');

INSERT INTO ADMIN_CODES (FirstName, LastName, Code)
VALUES ('Armond', 'Luthens', '01234');

UPDATE USERS SET Role='Admin' WHERE FirstName='Ar';

DELETE FROM USERS WHERE FirstName='Ar';
    
    
//PRODUCTS TABLE
INSERT INTO PRODUCTS (ProductID, Price, Qty, Description, Available, ProductName, ManufactureDate, ExpirationDate)
VALUES ('4', '49.99', '30', 'This comptuter is fully equipped with user needs to surf the web and do work on.', '1', 'Computers', DATE_SUB(CURDATE(),INTERVAL 30 DAY), DATE_ADD(CURDATE(),INTERVAL 30 DAY ));

     
-----------------------------------------------------*/
    
}
